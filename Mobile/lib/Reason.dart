import 'package:flutter/material.dart';

class Reason extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Visit Reason')),
      body: Center(child: Text('Welcome to the Visit Reason Page!')),
    );
  }
}
